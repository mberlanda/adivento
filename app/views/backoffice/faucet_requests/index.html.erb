<h1>Faucet Requests</h1>

<div class="adv-card adv-card--flush" style="margin-bottom:16px;">
  <div class="adv-card__head"><h2 style="margin:0;font-size:1.1rem;">Pending (<%= @pending.count %>)</h2></div>
  <% if @pending.any? %>
    <table class="adv-table">
      <thead>
        <tr>
          <th>Player</th>
          <th style="text-align:right;">Amount (ADIV)</th>
          <th>Requested at</th>
          <th></th>
        </tr>
      </thead>
      <tbody>
        <% @pending.each do |req| %>
          <tr>
            <td class="adv-muted"><%= req.user.email %></td>
            <td class="adv-num" style="text-align:right;"><%= number_with_delimiter(req.amount_minor) %></td>
            <td class="adv-muted adv-xs"><%= req.created_at.strftime("%Y-%m-%d %H:%M") %></td>
            <td>
              <%= button_to "Approve",
                    approve_backoffice_faucet_request_path(req),
                    method: :post,
                    class: "adv-btn adv-btn--primary adv-btn--sm",
                    data: { testid: "approve-#{req.id}" } %>
              <%= button_to "Reject",
                    reject_backoffice_faucet_request_path(req),
                    method: :post,
                    class: "adv-btn adv-btn--danger adv-btn--sm",
                    data: { testid: "reject-#{req.id}" } %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  <% else %>
    <p class="adv-muted" style="padding:16px;">No pending faucet requests.</p>
  <% end %>
</div>

<div class="adv-card adv-card--flush">
  <div class="adv-card__head"><h2 style="margin:0;font-size:1.1rem;">Recently Processed</h2></div>
  <% if @processed.any? %>
    <table class="adv-table">
      <thead>
        <tr>
          <th>Player</th>
          <th style="text-align:right;">Amount</th>
          <th>Status</th>
          <th>Reviewed by</th>
          <th>Processed at</th>
        </tr>
      </thead>
      <tbody>
        <% @processed.each do |req| %>
          <tr>
            <td class="adv-muted"><%= req.user.email %></td>
            <td class="adv-num" style="text-align:right;"><%= number_with_delimiter(req.amount_minor) %></td>
            <td><%= render "shared/adv_badge", status: req.status %></td>
            <td class="adv-muted"><%= req.reviewed_by&.email %></td>
            <td class="adv-muted adv-xs"><%= req.updated_at.strftime("%Y-%m-%d %H:%M") %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  <% else %>
    <p class="adv-muted" style="padding:16px;">No processed requests.</p>
  <% end %>
</div>
