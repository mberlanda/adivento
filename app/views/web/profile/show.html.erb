<h1 style="margin-bottom:20px;" data-testid="profile-title">My Profile</h1>

<div class="ds-grid ds-grid--auto" style="margin-bottom:20px;">
  <section class="ds-card" data-testid="wallet-balance-panel">
    <div class="ds-card__head"><h3 style="margin:0;">Wallet</h3></div>
    <div class="ds-grid ds-grid--auto" style="gap:12px;margin:12px 0;">
      <div class="ds-statcard">
        <div class="ds-stat__k">Available</div>
        <div class="ds-statcard__v ds-num" data-testid="wallet-available"><%= number_with_delimiter(@wallet.available_minor) %></div>
        <div class="ds-muted ds-xs">ADIV</div>
      </div>
      <div class="ds-statcard">
        <div class="ds-stat__k">Reserved</div>
        <div class="ds-statcard__v ds-num" data-testid="wallet-reserved"><%= number_with_delimiter(@wallet.reserved_minor) %></div>
        <div class="ds-muted ds-xs">ADIV</div>
      </div>
    </div>
    <details style="font-size:0.88rem;">
      <summary style="cursor:pointer;">Request more tokens</summary>
      <div style="margin-top:10px;padding:12px;border-radius:8px;border:1px solid var(--ds-border);"
           data-testid="faucet-form-section">
        <%= form_with url: web_faucet_requests_path, method: :post, data: { testid: "faucet-request-form" } do |f| %>
          <label class="ds-muted" style="font-size:0.85rem;">Amount (ADIV)
            <input type="number" name="amount_minor" value="10000" min="100" max="100000"
                   class="ds-input" style="margin-top:4px;" data-testid="faucet-amount">
          </label>
          <div style="margin-top:8px;">
            <%= f.submit "Request tokens", class: "ds-btn ds-btn--primary ds-btn--sm",
                  data: { testid: "faucet-submit" } %>
          </div>
        <% end %>
        <p class="ds-muted ds-xs" style="margin:6px 0 0;">Requests are reviewed by an admin. Approved funds appear in your wallet.</p>
      </div>
    </details>
  </section>

  <section class="ds-card" data-testid="pnl-panel">
    <div class="ds-card__head"><h3 style="margin:0;">P&amp;L Summary</h3></div>
    <div class="ds-grid ds-grid--auto" style="gap:12px;margin:12px 0;">
      <div class="ds-statcard">
        <div class="ds-stat__k">Open</div>
        <div class="ds-statcard__v ds-num" data-testid="pnl-open-count"><%= @pnl[:open_count] %></div>
      </div>
      <div class="ds-statcard">
        <div class="ds-stat__k">Won</div>
        <div class="ds-statcard__v ds-num ds-delta--up" data-testid="pnl-won-count"><%= @pnl[:won_count] %></div>
      </div>
      <div class="ds-statcard">
        <div class="ds-stat__k">Lost</div>
        <div class="ds-statcard__v ds-num ds-delta--down" data-testid="pnl-lost-count"><%= @pnl[:lost_count] %></div>
      </div>
    </div>
    <p style="margin:6px 0;font-size:0.88rem;">Staked: <strong data-testid="pnl-total-staked"><%= number_with_delimiter(@pnl[:total_staked]) %> ADIV</strong></p>
    <p style="margin:6px 0;font-size:0.88rem;">Returned: <strong data-testid="pnl-total-returned"><%= number_with_delimiter(@pnl[:total_returned]) %> ADIV</strong></p>
    <p style="margin:6px 0;font-size:0.88rem;">Net P&amp;L:
      <strong data-testid="pnl-net"
              class="ds-delta ds-delta--<%= @pnl[:net_pnl] >= 0 ? 'up' : 'down' %>">
        <%= @pnl[:net_pnl] >= 0 ? '+' : '' %><%= number_with_delimiter(@pnl[:net_pnl]) %> ADIV
      </strong>
    </p>
    <p style="margin:6px 0;font-size:0.88rem;">Win rate: <strong data-testid="pnl-win-rate"><%= @pnl[:win_rate] ? "#{@pnl[:win_rate]}%" : "—" %></strong></p>
  </section>
</div>

<section class="ds-card ds-card--flush" data-testid="bets-panel">
  <div class="ds-card__head" style="display:flex;justify-content:space-between;align-items:center;flex-wrap:wrap;gap:8px;">
    <h3 style="margin:0;">Bet History</h3>
    <div style="display:flex;gap:6px;flex-wrap:wrap;" data-testid="bet-status-filter">
      <% [['All', nil], ['Open', 'open'], ['Won', 'settled_win'], ['Lost', 'settled_loss'], ['Voided', 'voided']].each do |label, val| %>
        <% active = @status_filter == val %>
        <%= link_to label, web_profile_path(status: val),
              data: { testid: "filter-#{label.downcase}" },
              class: "ds-btn ds-btn--sm #{active ? 'ds-btn--primary' : 'ds-btn--ghost'}" %>
      <% end %>
    </div>
  </div>

  <% if @bets.any? %>
    <div style="overflow-x:auto;">
      <table class="ds-table" data-testid="bets-table">
        <thead>
          <tr>
            <th>Market</th>
            <th>Outcome</th>
            <th style="text-align:right;">Stake</th>
            <th style="text-align:right;">Payout</th>
            <th style="text-align:center;">Status</th>
            <th style="text-align:right;">Date</th>
          </tr>
        </thead>
        <tbody>
          <% @bets.each do |bet| %>
            <% cashed_out = bet.voided? && @cashout_payouts.key?(bet.id) %>
            <% display_payout = cashed_out ? @cashout_payouts[bet.id] : bet.potential_payout_minor %>
            <% display_status = cashed_out ? 'voided' : bet.status.sub('settled_', '') %>
            <tr data-testid="bet-row-<%= bet.id %>">
              <td><%= link_to bet.market.question.truncate(50), web_market_path(bet.market),
                    data: { testid: "bet-market-link-#{bet.id}" }, class: "ds-muted" %></td>
              <td><span class="ds-chip ds-chip--accent" data-testid="bet-leg-<%= bet.id %>"><%= bet.market_leg.label %></span></td>
              <td class="ds-num" style="text-align:right;" data-testid="bet-stake-<%= bet.id %>"><%= number_with_delimiter(bet.net_stake_minor) %></td>
              <td class="ds-num" style="text-align:right;" data-testid="bet-payout-<%= bet.id %>"><%= number_with_delimiter(display_payout) %></td>
              <td style="text-align:center;"><%= render "shared/adv_badge", status: display_status, label: (cashed_out ? 'Cashed out' : bet.status.humanize) %></td>
              <td class="ds-muted ds-xs" style="text-align:right;"><%= bet.created_at.strftime('%b %d, %Y') %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  <% else %>
    <p class="ds-muted" style="text-align:center;padding:24px 0;" data-testid="bets-empty">
      No bets found. <%= link_to "Browse markets", web_markets_path %> to place your first bet.
    </p>
  <% end %>
</section>
